// straw-android-options-menu-plugin

window.straw.optionsMenu = (function (straw, $) {
    'use strict';

    var PLUGIN_NAME = 'optionsMenu';

    var exports = {
    };

    return exports;
}(window.straw, window.$));
